let apiProjApi = new TempApi.ProjApi();import TempApi from '../src/index';window.onload = () => {let projId = window.location.pathname.replace('/Page3/','');apiProjApi.getproj( projId, (error, data, response) => { if (error) {console.error(error);} else { console.log('API called successfully. Returned data: ' + data); document.querySelector('[annotationname = title]').textContent = response.body.query.title ;document.querySelector('[annotationname = image]').src = response.body.query.image ;document.querySelector('[annotationname = abstract]').textContent = response.body.query.abstract ;}});};document.getElementById('if2k3').onclick = (event) => {
    event.preventDefault();
    let projId = window.location.pathname.replace('/Page3/','');apiProjApi.deleteproj( projId, (error, data, response) => { if (error) {console.error(error);} else { console.log('API called successfully.');{ location.href= '/Page1';}}});};